const {gets, print } = require('./index');
/*
console.log(funcoes);
console.log(funcoes.gets())
//Objeto destruido
const pessoa = {
    nome:'Marcus'
}
const {nome}= pessoa;
const nome = pessoa.nome;
*/